# Condor

This folder contains the files needed to run condor jobs. 

* `generate_config.py`: To run, do

  ```bash
  python3 ./condor/generate_config.py [input_root_path with root://cms-xrd-global.cern.ch/] ./condor/
  ```

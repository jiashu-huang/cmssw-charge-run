# Sample Job

This folder is a sample of a condor job. It is created by running 

```bash
./condor/make_condor_batch.sh ./batch_0_paths_EXAMPLE.txt ./sample-job/ --output-root-dir ../CMSSW_15_1_0_patch4/output/
```

... at the root directory of this repo.
